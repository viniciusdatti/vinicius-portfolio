/**
 * Compatibility barrel — imports resolve to the telemetryFieldCanvas/ folder.
 * Keeps legacy path `@/lib/telemetryFieldCanvas` stable for ESLint and bundlers.
 */

// TelemetryFieldCanvas
export * from './telemetryFieldCanvas/index';
