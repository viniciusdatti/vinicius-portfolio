# Dependencies
node_modules/
interfaces/web/node_modules/
.pnp
.pnp.js

# Python — virtualenv and bytecode (never commit)
venv/
backend/venv/
**/__pycache__/
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python
*.egg-info/
.eggs/
dist/
build/
*.egg

# Python — test and type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.hypothesis/

# Local databases (SQLite dev)
backend/portfolio.db
backend/*.db
*.db-journal
*.sqlite
*.sqlite3

# Testing (JS)
coverage/
interfaces/web/coverage/

# Production builds (frontend)
build/
interfaces/web/build/
interfaces/web/dist/

# Vite / Vitest local caches
interfaces/web/.vite/
.vite/
interfaces/web/playwright-report/
interfaces/web/test-results/

# Environment — never commit secrets (examples only)
.env
.env.*
!.env.example
!.env.*.example
backend/.env
interfaces/web/.env

# Local diff exports (review artifacts)
*.diff

# Local README / profile draft artifacts (never commit)
.tmp-*

# QA capture outputs (keep generated GIF tracked separately when intentional)
assets/live-lab-validation.png

# Logs
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# MCP / Cursor runtime artifacts (keep .cursor/rules tracked; ignore runtime only)
.cursor/playwright-output/
.cursor/mcp-shell*.log
mcp-shell.log
skills-src/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# One-off git history scrub scripts (local only; do not commit)
scripts/history-scrub/

# QA / audit artifacts (local only)
interfaces/web/qa-screenshots/
interfaces/web/qa-*.png
interfaces/web/scripts/**/*.png
interfaces/web/scripts/responsive-audit/
interfaces/web/scripts/visual-audit/

# npm lockfile (yarn only)
package-lock.json

# Misc
*.tsbuildinfo
